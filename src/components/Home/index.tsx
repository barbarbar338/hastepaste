import * as React from "react";
import classnames from "classnames";

export const Home = () => (
	<div className={classnames("home")}>
		<p>
			Quickly share a snippet of your code, note or paragraph with your friends!
		</p>
	</div>
);

export default Home;
